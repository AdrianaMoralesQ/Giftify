<%= form_with(model: public_gift) do |form| %>
  <% if public_gift.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(public_gift.errors.count, "error") %> prohibited this public_gift from being saved:</h2>

      <ul>
        <% public_gift.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-stretch'>
  <div>
    <%= form.label :recipient, style: "display: block" %>
    <%= form.select :recipient, options_for_select(['Baby', 'Children', 'Teens', 'Women', 'Men', 'Gender Neutral', 'Pets']) %>
  </div>

  <div>
    <%= form.label :gift_name, style: "display: block" %>
    <%= form.text_field :gift_name %>
  </div>

  <div>
    <%= form.label :gift_link, style: "display: block" %>
    <%= form.text_field :gift_link %>
  </div>

  <div>
    <%= form.label :gift_image, style: "display: block" %>
    <%= form.text_field :gift_image %>
  </div>

  <div>
    <%= form.submit class: 'btn'%>
  </div>
<% end %>
</div>